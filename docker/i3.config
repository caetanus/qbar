# Minimal-but-usable i3 for the nested-X (Xephyr) qbar repro session.
# Mod = Alt (Mod1): the Super/Win key is taken by the host desktop, so the
# friend's Debian-i3 magic key here is Alt.
set $mod Mod1

font pango:DejaVu Sans 9
floating_modifier $mod

# Wallpaper (it's a ricing bar — a black void doesn't sell it). Use the image on
# the volume if present, else fall back to a solid tone (wall.png isn't committed).
exec_always --no-startup-id sh -c 'feh --bg-fill /src/docker/wall.png 2>/dev/null || xsetroot -solid "#1a2433"'

# --- launchers ---------------------------------------------------------------
bindsym $mod+Return exec --no-startup-id tilix
bindsym $mod+d      exec --no-startup-id rofi -show drun -show-icons
bindsym $mod+Shift+q kill

# --- focus (arrows + vim) ----------------------------------------------------
bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# --- move --------------------------------------------------------------------
bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Right move right

# --- layout ------------------------------------------------------------------
bindsym $mod+f fullscreen toggle
bindsym $mod+e layout toggle split
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+space floating toggle
bindsym $mod+v split v
bindsym $mod+b split h

# --- workspaces --------------------------------------------------------------
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5

# --- session -----------------------------------------------------------------
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# No bar block — qbar IS the bar, launched separately by inside.sh.
